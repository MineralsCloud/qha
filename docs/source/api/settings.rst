Data model for user computational settings
******************************************

In this module, we give a simple computational settings ``DEFAULT_SETTING``, which will
be used by default in the following calculation, if the users do not specify settings themselves.
If they want, they can write some settings in a YAML file.

.. automodule:: qha

.. automodule:: qha.settings
   :members:
   :undoc-members:
